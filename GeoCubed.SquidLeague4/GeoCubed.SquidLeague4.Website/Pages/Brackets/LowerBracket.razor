<div class="text-center">
    <h3 class="m-4">Lower Bracket</h3>

    <iframe src="https://challonge.com/SL2021lower/module" width="100%" height="500" frameborder="0" scrolling="auto" allowtransparency="true"></iframe>

    <h4 class="m-2">Matches</h4>
    @if (this.lr1.Any())
    {
        <h4 class="m-2"><em>Round 1</em></h4>
        <div class="d-flex flex-wrap justify-content-center">
            @foreach (var match in this.lr1)
            {
                <div class="card m-2 adminCard" style="min-width:200px;cursor:pointer;" @onclick="() => this.NavigateTo(match)">
                    <div class="card-body">
                        <div class="d-flex flex-row pb-2">
                            <div class="flex-grow-1 pr-2">@match.HomeTeamName</div>
                            <div class="border-left pl-2">@match.HomeTeamScore</div>
                        </div>
                        <div class="d-flex flex-row">
                            <div class="flex-grow-1 pr-2">@match.AwayTeamName</div>
                            <div class="border-left pl-2">@match.AwayTeamScore</div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }

    @if (this.lqf.Any())
    {
        <h4 class="m-2"><em>Quarter Finals</em></h4>
        <div class="d-flex flex-wrap justify-content-center">
            @foreach (var match in this.lqf)
            {
                <div class="card m-2 adminCard" style="min-width:200px;cursor:pointer;" @onclick="() => this.NavigateTo(match)">
                    <div class="card-body">
                        <div class="d-flex flex-row pb-2">
                            <div class="flex-grow-1 pr-2">@match.HomeTeamName</div>
                            <div class="border-left pl-2">@match.HomeTeamScore</div>
                        </div>
                        <div class="d-flex flex-row">
                            <div class="flex-grow-1 pr-2">@match.AwayTeamName</div>
                            <div class="border-left pl-2">@match.AwayTeamScore</div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }

    @if (this.lsf.Any())
    {
        <h4 class="m-2"><em>Semi Finals</em></h4>
        <div class="d-flex flex-wrap justify-content-center">
            @foreach (var match in this.lsf)
            {
                <div class="card m-2 adminCard" style="min-width:200px;cursor:pointer;" @onclick="() => this.NavigateTo(match)">
                    <div class="card-body">
                        <div class="d-flex flex-row pb-2">
                            <div class="flex-grow-1 pr-2">@match.HomeTeamName</div>
                            <div class="border-left pl-2">@match.HomeTeamScore</div>
                        </div>
                        <div class="d-flex flex-row">
                            <div class="flex-grow-1 pr-2">@match.AwayTeamName</div>
                            <div class="border-left pl-2">@match.AwayTeamScore</div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }

    @if (this.lgf.Any())
    {
        <h4 class="m-2"><em>Grand Finals</em></h4>
        <div class="d-flex flex-wrap justify-content-center">
            @foreach (var match in this.lgf)
            {
                <div class="card m-2 adminCard" style="min-width:200px;cursor:pointer;" @onclick="() => this.NavigateTo(match)">
                    <div class="card-body">
                        <div class="d-flex flex-row pb-2">
                            <div class="flex-grow-1 pr-2">@match.HomeTeamName</div>
                            <div class="border-left pl-2">@match.HomeTeamScore</div>
                        </div>
                        <div class="d-flex flex-row">
                            <div class="flex-grow-1 pr-2">@match.AwayTeamName</div>
                            <div class="border-left pl-2">@match.AwayTeamScore</div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>
